import { TodoBoldDirective } from './todo-bold.directive';

describe('TodoBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoBoldDirective();
    expect(directive).toBeTruthy();
  });
});
